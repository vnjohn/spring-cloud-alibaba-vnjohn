server:
  port: 9001
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  application:
    name: alibaba-provider

management:
  endpoint:
    web:
      exposure:
        include:'*'
