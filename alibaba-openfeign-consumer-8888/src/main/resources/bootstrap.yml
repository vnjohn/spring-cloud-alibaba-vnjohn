server:
  port: 8888
spring:
  application:
    name: alibaba-openfeign-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'

# ??feign???????(OpenFeign????ribbon)
ribbon:
  # ??????????????????????????,?????????
  ReadTimeout: 5000
  # ????????????????????????
  ConnectTimeout: 5000

logging:
  level:
    # openFeign ?????????????
    com.vnjohn.openfeign.service.OpenFeignService: debug