server:
  port: 8803
spring:
  application:
    name: alibaba-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

# 消费者将要去访问的微服务名称（注册成功的 Nacos 微服务提供者）
service-url:
  nacos-user-service: http://alibaba-provider